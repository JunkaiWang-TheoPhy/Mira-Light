# 08_20260425_043832_01 解读

## 基本信息

- 视频文件：`08_20260425_043832_01.mp4`
- 时长：`39.6s`
- 拼图：`08_20260425_043832_01_contact.jpg`
- 单帧目录：`08_20260425_043832_01/`

## 当前判断

这是一条很强的长版 tabletop/object follow 参考。

相比 `04`，它的特点是：

- 跟随路径更长
- far-side hold 更明显
- 结尾不急着回中

## 分阶段解读

### 阶段 1：面向近桌面交互区

- 时间范围：约 `0s - 8s`
- 观察：开场面向 laptop 和 bottle 附近的小范围交互区。
- 动作含义：scene 从明确的 workspace focus 开始。

### 阶段 2：目标向右移动，机器人开始连续跟随

- 时间范围：约 `8s - 20s`
- 观察：随着 guest 手/物体右移，机器人沿桌面方向持续移动。
- 动作含义：不是一次 reach，而是连续 tracking。

### 阶段 3：远侧定位与保持

- 时间范围：约 `20s - 31s`
- 观察：进入 far-side pose 后没有马上回来，而是在那边维持。
- 动作含义：这一步很适合定义“follow 到位后的 hold”。

### 阶段 4：局部修正后结束

- 时间范围：约 `31s - 39.6s`
- 观察：收尾不是回中心，而是在目标区附近做小修正并结束。
- 动作含义：scene 结束时仍然保持目标相关性。

## 动作语义

这条视频非常适合说明：

- continuous tabletop follow
- target-side hold
- local correction near final target

## 写脚本时建议保留

1. 必须保留“跟到 far side 后继续 hold”。
2. 不要把整条压缩成一次 reach。
3. 收尾可以保留 target-oriented end pose。

## 不建议直接照搬的地方

1. 不建议删掉中段连续 tracking，只留下末端姿态。
2. 不建议让结尾强行回正，否则会损失 scene 语义。

## 对后续脚本设计的启发

适合压成：

- `focus_workspace()`
- `track_rightward_path()`
- `hold_far_target_pose()`
- `micro_correct_near_target()`

如果需要做 tabletop/object follow 的长版 fixed demo，这条视频比 `04` 更适合当主参考。
